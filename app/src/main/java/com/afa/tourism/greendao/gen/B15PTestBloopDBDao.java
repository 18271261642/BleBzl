package com.afa.tourism.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ble.blebzl.b15p.b15pdb.B15PTestBloopDB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "B15_PTEST_BLOOP_DB".
*/
public class B15PTestBloopDBDao extends AbstractDao<B15PTestBloopDB, Long> {

    public static final String TABLENAME = "B15_PTEST_BLOOP_DB";

    /**
     * Properties of entity B15PTestBloopDB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property DevicesMac = new Property(1, String.class, "devicesMac", false, "DEVICES_MAC");
        public final static Property BloodNumberH = new Property(2, int.class, "bloodNumberH", false, "BLOOD_NUMBER_H");
        public final static Property BloodNumberL = new Property(3, int.class, "bloodNumberL", false, "BLOOD_NUMBER_L");
    }


    public B15PTestBloopDBDao(DaoConfig config) {
        super(config);
    }
    
    public B15PTestBloopDBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"B15_PTEST_BLOOP_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"DEVICES_MAC\" TEXT," + // 1: devicesMac
                "\"BLOOD_NUMBER_H\" INTEGER NOT NULL ," + // 2: bloodNumberH
                "\"BLOOD_NUMBER_L\" INTEGER NOT NULL );"); // 3: bloodNumberL
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"B15_PTEST_BLOOP_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, B15PTestBloopDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
        stmt.bindLong(3, entity.getBloodNumberH());
        stmt.bindLong(4, entity.getBloodNumberL());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, B15PTestBloopDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
        stmt.bindLong(3, entity.getBloodNumberH());
        stmt.bindLong(4, entity.getBloodNumberL());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public B15PTestBloopDB readEntity(Cursor cursor, int offset) {
        B15PTestBloopDB entity = new B15PTestBloopDB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // devicesMac
            cursor.getInt(offset + 2), // bloodNumberH
            cursor.getInt(offset + 3) // bloodNumberL
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, B15PTestBloopDB entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDevicesMac(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setBloodNumberH(cursor.getInt(offset + 2));
        entity.setBloodNumberL(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(B15PTestBloopDB entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(B15PTestBloopDB entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(B15PTestBloopDB entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
