package com.afa.tourism.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ble.blebzl.b15p.b15pdb.B15PBloodDB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "B15_PBLOOD_DB".
*/
public class B15PBloodDBDao extends AbstractDao<B15PBloodDB, Long> {

    public static final String TABLENAME = "B15_PBLOOD_DB";

    /**
     * Properties of entity B15PBloodDB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property DevicesMac = new Property(1, String.class, "devicesMac", false, "DEVICES_MAC");
        public final static Property BloodData = new Property(2, String.class, "bloodData", false, "BLOOD_DATA");
        public final static Property BloodTime = new Property(3, String.class, "bloodTime", false, "BLOOD_TIME");
        public final static Property BloodNumberH = new Property(4, int.class, "bloodNumberH", false, "BLOOD_NUMBER_H");
        public final static Property BloodNumberL = new Property(5, int.class, "bloodNumberL", false, "BLOOD_NUMBER_L");
        public final static Property IsUpdata = new Property(6, int.class, "isUpdata", false, "IS_UPDATA");
    }


    public B15PBloodDBDao(DaoConfig config) {
        super(config);
    }
    
    public B15PBloodDBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"B15_PBLOOD_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"DEVICES_MAC\" TEXT," + // 1: devicesMac
                "\"BLOOD_DATA\" TEXT," + // 2: bloodData
                "\"BLOOD_TIME\" TEXT," + // 3: bloodTime
                "\"BLOOD_NUMBER_H\" INTEGER NOT NULL ," + // 4: bloodNumberH
                "\"BLOOD_NUMBER_L\" INTEGER NOT NULL ," + // 5: bloodNumberL
                "\"IS_UPDATA\" INTEGER NOT NULL );"); // 6: isUpdata
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"B15_PBLOOD_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, B15PBloodDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String bloodData = entity.getBloodData();
        if (bloodData != null) {
            stmt.bindString(3, bloodData);
        }
 
        String bloodTime = entity.getBloodTime();
        if (bloodTime != null) {
            stmt.bindString(4, bloodTime);
        }
        stmt.bindLong(5, entity.getBloodNumberH());
        stmt.bindLong(6, entity.getBloodNumberL());
        stmt.bindLong(7, entity.getIsUpdata());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, B15PBloodDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String bloodData = entity.getBloodData();
        if (bloodData != null) {
            stmt.bindString(3, bloodData);
        }
 
        String bloodTime = entity.getBloodTime();
        if (bloodTime != null) {
            stmt.bindString(4, bloodTime);
        }
        stmt.bindLong(5, entity.getBloodNumberH());
        stmt.bindLong(6, entity.getBloodNumberL());
        stmt.bindLong(7, entity.getIsUpdata());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public B15PBloodDB readEntity(Cursor cursor, int offset) {
        B15PBloodDB entity = new B15PBloodDB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // devicesMac
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // bloodData
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // bloodTime
            cursor.getInt(offset + 4), // bloodNumberH
            cursor.getInt(offset + 5), // bloodNumberL
            cursor.getInt(offset + 6) // isUpdata
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, B15PBloodDB entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDevicesMac(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setBloodData(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setBloodTime(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setBloodNumberH(cursor.getInt(offset + 4));
        entity.setBloodNumberL(cursor.getInt(offset + 5));
        entity.setIsUpdata(cursor.getInt(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(B15PBloodDB entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(B15PBloodDB entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(B15PBloodDB entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
