package com.afa.tourism.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ble.blebzl.b15p.b15pdb.B15PStepDB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "B15_PSTEP_DB".
*/
public class B15PStepDBDao extends AbstractDao<B15PStepDB, Long> {

    public static final String TABLENAME = "B15_PSTEP_DB";

    /**
     * Properties of entity B15PStepDB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property DevicesMac = new Property(1, String.class, "devicesMac", false, "DEVICES_MAC");
        public final static Property StepData = new Property(2, String.class, "stepData", false, "STEP_DATA");
        public final static Property StepTime = new Property(3, String.class, "stepTime", false, "STEP_TIME");
        public final static Property StepItemNumber = new Property(4, int.class, "stepItemNumber", false, "STEP_ITEM_NUMBER");
        public final static Property IsUpdata = new Property(5, int.class, "isUpdata", false, "IS_UPDATA");
    }


    public B15PStepDBDao(DaoConfig config) {
        super(config);
    }
    
    public B15PStepDBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"B15_PSTEP_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"DEVICES_MAC\" TEXT," + // 1: devicesMac
                "\"STEP_DATA\" TEXT," + // 2: stepData
                "\"STEP_TIME\" TEXT," + // 3: stepTime
                "\"STEP_ITEM_NUMBER\" INTEGER NOT NULL ," + // 4: stepItemNumber
                "\"IS_UPDATA\" INTEGER NOT NULL );"); // 5: isUpdata
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"B15_PSTEP_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, B15PStepDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String stepData = entity.getStepData();
        if (stepData != null) {
            stmt.bindString(3, stepData);
        }
 
        String stepTime = entity.getStepTime();
        if (stepTime != null) {
            stmt.bindString(4, stepTime);
        }
        stmt.bindLong(5, entity.getStepItemNumber());
        stmt.bindLong(6, entity.getIsUpdata());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, B15PStepDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String stepData = entity.getStepData();
        if (stepData != null) {
            stmt.bindString(3, stepData);
        }
 
        String stepTime = entity.getStepTime();
        if (stepTime != null) {
            stmt.bindString(4, stepTime);
        }
        stmt.bindLong(5, entity.getStepItemNumber());
        stmt.bindLong(6, entity.getIsUpdata());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public B15PStepDB readEntity(Cursor cursor, int offset) {
        B15PStepDB entity = new B15PStepDB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // devicesMac
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // stepData
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // stepTime
            cursor.getInt(offset + 4), // stepItemNumber
            cursor.getInt(offset + 5) // isUpdata
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, B15PStepDB entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDevicesMac(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setStepData(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setStepTime(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setStepItemNumber(cursor.getInt(offset + 4));
        entity.setIsUpdata(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(B15PStepDB entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(B15PStepDB entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(B15PStepDB entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
