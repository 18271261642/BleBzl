package com.afa.tourism.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ble.blebzl.b15p.b15pdb.B15PHeartDB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "B15_PHEART_DB".
*/
public class B15PHeartDBDao extends AbstractDao<B15PHeartDB, Long> {

    public static final String TABLENAME = "B15_PHEART_DB";

    /**
     * Properties of entity B15PHeartDB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property DevicesMac = new Property(1, String.class, "devicesMac", false, "DEVICES_MAC");
        public final static Property HeartData = new Property(2, String.class, "heartData", false, "HEART_DATA");
        public final static Property HeartTime = new Property(3, String.class, "heartTime", false, "HEART_TIME");
        public final static Property HeartNumber = new Property(4, int.class, "heartNumber", false, "HEART_NUMBER");
        public final static Property IsUpdata = new Property(5, int.class, "isUpdata", false, "IS_UPDATA");
    }


    public B15PHeartDBDao(DaoConfig config) {
        super(config);
    }
    
    public B15PHeartDBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"B15_PHEART_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"DEVICES_MAC\" TEXT," + // 1: devicesMac
                "\"HEART_DATA\" TEXT," + // 2: heartData
                "\"HEART_TIME\" TEXT," + // 3: heartTime
                "\"HEART_NUMBER\" INTEGER NOT NULL ," + // 4: heartNumber
                "\"IS_UPDATA\" INTEGER NOT NULL );"); // 5: isUpdata
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"B15_PHEART_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, B15PHeartDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String heartData = entity.getHeartData();
        if (heartData != null) {
            stmt.bindString(3, heartData);
        }
 
        String heartTime = entity.getHeartTime();
        if (heartTime != null) {
            stmt.bindString(4, heartTime);
        }
        stmt.bindLong(5, entity.getHeartNumber());
        stmt.bindLong(6, entity.getIsUpdata());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, B15PHeartDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String heartData = entity.getHeartData();
        if (heartData != null) {
            stmt.bindString(3, heartData);
        }
 
        String heartTime = entity.getHeartTime();
        if (heartTime != null) {
            stmt.bindString(4, heartTime);
        }
        stmt.bindLong(5, entity.getHeartNumber());
        stmt.bindLong(6, entity.getIsUpdata());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public B15PHeartDB readEntity(Cursor cursor, int offset) {
        B15PHeartDB entity = new B15PHeartDB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // devicesMac
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // heartData
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // heartTime
            cursor.getInt(offset + 4), // heartNumber
            cursor.getInt(offset + 5) // isUpdata
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, B15PHeartDB entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDevicesMac(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setHeartData(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setHeartTime(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setHeartNumber(cursor.getInt(offset + 4));
        entity.setIsUpdata(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(B15PHeartDB entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(B15PHeartDB entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(B15PHeartDB entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
