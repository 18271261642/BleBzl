package com.afa.tourism.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ble.blebzl.b15p.b15pdb.B15PSleepDB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "B15_PSLEEP_DB".
*/
public class B15PSleepDBDao extends AbstractDao<B15PSleepDB, Long> {

    public static final String TABLENAME = "B15_PSLEEP_DB";

    /**
     * Properties of entity B15PSleepDB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property DevicesMac = new Property(1, String.class, "devicesMac", false, "DEVICES_MAC");
        public final static Property SleepData = new Property(2, String.class, "sleepData", false, "SLEEP_DATA");
        public final static Property SleepTime = new Property(3, String.class, "sleepTime", false, "SLEEP_TIME");
        public final static Property StartTime = new Property(4, String.class, "startTime", false, "START_TIME");
        public final static Property EndTime = new Property(5, String.class, "endTime", false, "END_TIME");
        public final static Property SleepType = new Property(6, String.class, "sleepType", false, "SLEEP_TYPE");
        public final static Property IsUpdata = new Property(7, int.class, "isUpdata", false, "IS_UPDATA");
    }


    public B15PSleepDBDao(DaoConfig config) {
        super(config);
    }
    
    public B15PSleepDBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"B15_PSLEEP_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"DEVICES_MAC\" TEXT," + // 1: devicesMac
                "\"SLEEP_DATA\" TEXT," + // 2: sleepData
                "\"SLEEP_TIME\" TEXT," + // 3: sleepTime
                "\"START_TIME\" TEXT," + // 4: startTime
                "\"END_TIME\" TEXT," + // 5: endTime
                "\"SLEEP_TYPE\" TEXT," + // 6: sleepType
                "\"IS_UPDATA\" INTEGER NOT NULL );"); // 7: isUpdata
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"B15_PSLEEP_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, B15PSleepDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String sleepData = entity.getSleepData();
        if (sleepData != null) {
            stmt.bindString(3, sleepData);
        }
 
        String sleepTime = entity.getSleepTime();
        if (sleepTime != null) {
            stmt.bindString(4, sleepTime);
        }
 
        String startTime = entity.getStartTime();
        if (startTime != null) {
            stmt.bindString(5, startTime);
        }
 
        String endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindString(6, endTime);
        }
 
        String sleepType = entity.getSleepType();
        if (sleepType != null) {
            stmt.bindString(7, sleepType);
        }
        stmt.bindLong(8, entity.getIsUpdata());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, B15PSleepDB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String devicesMac = entity.getDevicesMac();
        if (devicesMac != null) {
            stmt.bindString(2, devicesMac);
        }
 
        String sleepData = entity.getSleepData();
        if (sleepData != null) {
            stmt.bindString(3, sleepData);
        }
 
        String sleepTime = entity.getSleepTime();
        if (sleepTime != null) {
            stmt.bindString(4, sleepTime);
        }
 
        String startTime = entity.getStartTime();
        if (startTime != null) {
            stmt.bindString(5, startTime);
        }
 
        String endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindString(6, endTime);
        }
 
        String sleepType = entity.getSleepType();
        if (sleepType != null) {
            stmt.bindString(7, sleepType);
        }
        stmt.bindLong(8, entity.getIsUpdata());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public B15PSleepDB readEntity(Cursor cursor, int offset) {
        B15PSleepDB entity = new B15PSleepDB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // devicesMac
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // sleepData
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // sleepTime
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // startTime
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // endTime
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // sleepType
            cursor.getInt(offset + 7) // isUpdata
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, B15PSleepDB entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDevicesMac(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setSleepData(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSleepTime(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setStartTime(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setEndTime(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setSleepType(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setIsUpdata(cursor.getInt(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(B15PSleepDB entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(B15PSleepDB entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(B15PSleepDB entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
